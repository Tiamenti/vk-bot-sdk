<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Режим работы бота
    |--------------------------------------------------------------------------
    |
    | 'callback'  — получение событий через Callback API (вебхук)
    | 'longpoll'  — Long Poll (периодические запросы к VK)
    |
    */
    'mode' => env('VK_BOT_MODE', 'callback'),

    /*
    |--------------------------------------------------------------------------
    | Токен сообщества
    |--------------------------------------------------------------------------
    |
    | Токен доступа сообщества ВКонтакте. Используется для вызова методов API.
    | Генерируется в Управление > Настройки > Работа с API.
    |
    */
    'token' => env('VK_BOT_TOKEN', ''),

    /*
    |--------------------------------------------------------------------------
    | ID сообщества
    |--------------------------------------------------------------------------
    */
    'group_id' => env('VK_BOT_GROUP_ID', 0),

    /*
    |--------------------------------------------------------------------------
    | Секретный ключ Callback API
    |--------------------------------------------------------------------------
    |
    | Секретный ключ из настроек Callback API. Используется для проверки
    | подлинности входящих запросов.
    |
    */
    'secret' => env('VK_BOT_SECRET', ''),

    /*
    |--------------------------------------------------------------------------
    | Строка подтверждения Callback API
    |--------------------------------------------------------------------------
    |
    | Строка, которую VK ожидает получить в ответ на первый запрос.
    | Генерируется в настройках Callback API сообщества.
    |
    */
    'confirmation_token' => env('VK_BOT_CONFIRMATION_TOKEN', ''),

    /*
    |--------------------------------------------------------------------------
    | Версия VK API
    |--------------------------------------------------------------------------
    */
    'api_version' => env('VK_API_VERSION', '5.199'),

    /*
    |--------------------------------------------------------------------------
    | Маршруты (файл с обработчиками)
    |--------------------------------------------------------------------------
    */
    'routes' => [
        'path' => env('VK_BOT_ROUTES_PATH', 'routes/vk.php'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Беседы (Conversations)
    |--------------------------------------------------------------------------
    */
    'conversations' => [

        /*
         | Драйвер хранения состояния диалога.
         | 'cache'    — Laravel Cache (по умолчанию)
         | 'database' — таблица в базе данных
         */
        'driver' => env('VK_BOT_CONVERSATIONS_DRIVER', 'cache'),

        /*
         | Время жизни состояния диалога (в минутах).
         */
        'ttl' => env('VK_BOT_CONVERSATIONS_TTL', 60),

        /*
         | Таблица для хранения состояния диалога (только для database-драйвера).
         */
        'table' => env('VK_BOT_CONVERSATIONS_TABLE', 'vk_conversations'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Фасад Vk::
    |--------------------------------------------------------------------------
    |
    | Установите в false, чтобы отключить регистрацию фасада.
    |
    */
    'facade' => env('VK_BOT_FACADE', true),

    /*
    |--------------------------------------------------------------------------
    | Long Poll настройки
    |--------------------------------------------------------------------------
    */
    'longpoll' => [

        /*
         | Время ожидания ответа от сервера Long Poll (в секундах).
         | Максимальное значение: 90.
         */
        'wait' => env('VK_BOT_LONGPOLL_WAIT', 25),

        /*
         | Задержка между неудачными попытками (в секундах).
         */
        'retry_delay' => env('VK_BOT_LONGPOLL_RETRY_DELAY', 5),
    ],

    /*
    |--------------------------------------------------------------------------
    | HTTP настройки
    |--------------------------------------------------------------------------
    */
    'http' => [

        /*
         | Таймаут HTTP-запросов к VK API (в секундах).
         */
        'timeout' => env('VK_BOT_HTTP_TIMEOUT', 30),
    ],

];
